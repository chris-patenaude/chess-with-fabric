export default class Move {
    constructor(piece, originSquare, destinationSquare) {
        this.piece = piece;
        this.originSquare = originSquare;
        this.destinationSquare = destinationSquare;
    }

    toString() {
        // TODO
    }
}